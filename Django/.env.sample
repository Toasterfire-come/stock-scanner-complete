# Django Environment Sample
# Copy to .env and adjust values as needed

DEBUG=False
DJANGO_SETTINGS_MODULE=stockscanner_django.settings

# Database (XAMPP)
DB_ENGINE=django.db.backends.mysql
DB_NAME=stockscanner
DB_USER=stockscanner_user
DB_PASSWORD=StockScanner_2025!
DB_HOST=localhost
DB_PORT=3306

# Optional: Admin credentials for DB creation (if root has a password)
# MYSQL_ADMIN_USER=root
# MYSQL_ADMIN_PASSWORD=your_root_password